/* 
 *
 * David Sanchez
 * Newcastle University. 2017
 * 
 * This is a Sample object is used to store attribute values and logic
 * 
 * @param {type} id
 * @param {type} logic
 * @param {type} attributeValues
 * @returns {Sample}
 */
var Sample = function (id, logic, attributeValues) {
    this.id=id;
    this.logic = logic;
    this.attributeValues = attributeValues;
};
